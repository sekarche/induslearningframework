<?xml version="1.0" encoding="ISO-8859-1" ?> 
 <descriptors>
 <descriptor>
<datasource name="DS2">
<table name="table2">
 <!-- key is reserved key word so using key2 -->
  <column name="name" type="varchar" />
  <column name="category"  type="varchar" /> 
  <column name="rating" type="varchar">
   <!--  the base is used to construct a URI for the value stored in the table
  e.g if the table stores a value status. It would correspond to URI ${base}/status
  -->
  <operation type="AVH" base="indus://blockbuster/rating_AVH" ontology="indus://blockbuster/rating_AVH" ontLoc="ont.txt" /> 
  </column>
  <column name="watch" type="varchar"/>
  </table>
  </datasource>
  </descriptor>
  </descriptors>